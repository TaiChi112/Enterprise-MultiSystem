# Role & Objective
You are an Expert Enterprise Cloud Architect & Go Developer.
Your objective is to finalize the core internal enterprise systems of our Go Monorepo Microservices architecture by introducing HRM (Human Resource Management) and ERP (Enterprise Resource Planning) services.

# 1. System Context & Target Architecture
Currently, our ecosystem handles sales, customers, inventory, and supply chain. 
To achieve comprehensive corporate management, we need:
1.  **hrm-api**: Manages Employee profiles, roles, and basic payroll data.
2.  **erp-api**: Acts as a financial aggregator. It does not duplicate transactional data. Instead, it pulls revenue data from `oms-api`/`pos-api`, cost of goods from `scm-api`, and payroll costs from `hrm-api` to generate a Profit & Loss (P&L) summary.

# 2. Tech Stack & Rules
- Backend: Go (Golang)
- Architecture: Monorepo structure inside the `services/` directory. Follow the established Clean Architecture (Handler -> Service -> Repository).
- Communication: Service-to-service HTTP calls via internal network.
- Standards: Implement robust error handling for inter-service communication (e.g., timeouts, retries).

# 3. Execution Plan (Todo List) & Expected Results
Please execute this project step-by-step. Do not proceed to the next step until I confirm the result of the current step.

- [ ] STEP 1: Implement the HRM Service
  - Task: Create `services/hrm-api/`. Define Domain Models for `Employee` (ID, Name, Role, BaseSalary). Create standard CRUD endpoints for Employee management.
  - Expected Result: A running HRM service. I can create an employee via API securely.

- [ ] STEP 2: Implement the ERP Financial Service
  - Task: Create `services/erp-api/`. Define a Domain Model for `FinancialSummary` (Revenue, COGS, PayrollCost, NetProfit). Create an endpoint (e.g., `GET /erp/financial-summary`).
  - Expected Result: A running ERP service with a skeleton endpoint prepared to aggregate data.

- [ ] STEP 3: API Gateway Integration
  - Task: Update `services/api-gateway/` to route `/hrm/*` to the `hrm-api` and `/erp/*` to the `erp-api`. Ensure these routes are protected by the IAM JWT middleware. Update `docker-compose.yml` to include the new services.
  - Expected Result: I can access the HRM and ERP endpoints securely via the API Gateway port.

- [ ] STEP 4: Data Aggregation Flow (ERP calls OMS, SCM, HRM)
  - Task: Implement the logic in `erp-api`'s financial summary endpoint. It must make concurrent HTTP requests (using Go's Goroutines and WaitGroup) to `oms-api` (for total sales), `scm-api` (for total purchase costs), and `hrm-api` (for total payroll). Calculate the Net Profit and return the summary.
  - Expected Result: A complete financial report generated by aggregating data across the microservices ecosystem.

# Instruction to Agent:
Acknowledge this master prompt by explaining the concept of an "Aggregator" pattern used in the ERP service for STEP 4. Detail how Goroutines will improve the performance of this aggregation. Do not write any code yet. Ask for my permission to begin STEP 1.